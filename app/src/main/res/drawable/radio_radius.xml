<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="50dp" />
    <solid android:color="@color/background_radioButton" />
    <stroke
         android:width="2dp"
        android:color="@color/background_radioButton" />
</shape>